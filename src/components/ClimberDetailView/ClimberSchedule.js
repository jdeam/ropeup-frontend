import React from 'react';
import { scheduleMap } from '../../util/schedules';

const ClimberSchedule = ({ userSched, climberSched }) => {
  

  return (
    <div></div>
  );
}

export default ClimberSchedule;
